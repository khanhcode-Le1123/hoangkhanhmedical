# PostgreSQL connection string. Never commit the real value.
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/hoangkhanhmedical?schema=public"

# At least 32 random characters. Used to sign the HttpOnly session cookie.
AUTH_SECRET="replace-with-a-long-random-secret"

# Canonical origin for metadata and deployment checks.
NEXT_PUBLIC_SITE_URL="http://localhost:3000"
